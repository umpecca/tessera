# Task 016: Windows tray ICO asset

Status: complete

Use the supplied `web/assets/icon-192.ico` for the Windows system-tray icon,
while retaining the PNG tray asset on macOS and Linux. Add a platform-specific
asset selector and verify the Windows build accepts the embedded ICO without
the systray `SetIcon` error.
